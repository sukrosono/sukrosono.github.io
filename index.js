(function() {
  var adition, win;

  adition = function(input) {
    return input * input;
  };

  if (typeof a !== "undefined" && a !== null) {
    console.log('From freedom came elegance');
  }

  win = {
    persistent: true,
    will: true,
    commitment: true
  };

}).call(this);
