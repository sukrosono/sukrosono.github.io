(function() {
  var adition, win;

  adition = function(input) {
    return input * input;
  };

  if (typeof a !== "undefined" && a !== null) {
    console.log('err');
  }

  win = {
    persistent: true,
    will: true,
    commitment: true
  };

}).call(this);
